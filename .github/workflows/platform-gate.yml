name: Platform Gate

on:
  pull_request:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  platform:
    name: Platform integrity
    uses: ./.github/workflows/platform-integrity.yml
