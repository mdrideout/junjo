[project]
name = "junjo-docs-migration-tooling"
version = "0.1.0"
description = "Isolated mechanical documentation migration tooling for Junjo."
requires-python = ">=3.13"
dependencies = [
    "rst-to-myst==0.4.0",
]

[tool.uv]
package = false
