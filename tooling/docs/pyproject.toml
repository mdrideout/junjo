[project]
name = "junjo-legacy-docs-converter"
version = "0.1.0"
description = "Isolated converter for Junjo releases that predate owned Markdown."
requires-python = ">=3.13"
dependencies = [
    "rst-to-myst==0.4.0",
]

[tool.uv]
package = false
