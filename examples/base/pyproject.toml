# examples/dev/pyproject.toml
[project]
name = "app"
version = "0.1.0"
description = "Development example for junjo"
requires-python = ">=3.11"
dependencies = [
  "junjo @ file:../../"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
